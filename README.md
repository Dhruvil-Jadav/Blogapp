# Blogapp

Full-stack blog with JWT authentication.

**Stack:** React (Vite) · Node.js/Express · MySQL (users) · MongoDB (posts) · JWT

## Structure
- `server/` — Express REST API
- `client/` — React frontend

## Run locally
1. `cd server && npm install && npm run dev`
2. `cd client && npm install && npm run dev`